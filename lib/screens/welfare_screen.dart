import 'package:flutter/material.dart';

class WelfareScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Loans")),
      body: Center(child: Text("Loans screen coming soon...")),
    );
  }
}
